export enum MenuState {
    Onboarding,
    Init,
    Welcome,
    Main,
    Settings
}

export struct LibrarySong {
    path: string,
    title: string,
    length: int,
    artist: string,
    album: string,
    album_artist: string,
    track_number: int,
    year: int,
    cover_path: string,
    cover: image
}

export enum UIVolumeMode {
    Linear,
    Decibels
}
