export enum MenuState {
    Onboarding,
    Init,
    Welcome,
    Main
}

export struct LibrarySong {
    path: string,
    title: string,
    length: int,
    artist: string,
    album: string,
    album_artist: string,
    track_number: int,
    year: int,
    cover: image
}
